fun main(args: Array<String>)
{
//    && == and  || == or   ! == not
    var username = readLine()
    var password = readLine()
    if(username =="admin" && password =="1234")
        println("Go in!")
    else
        println("Go away!")
    //admin
    //1234
    //Go in!
}